import type { GameEventModule } from "../uno/UnoEventModule";

export const BjEventModule: GameEventModule = {
    initialize(room, $) {
        const unlisteners = [];


        return unlisteners;
    }
}