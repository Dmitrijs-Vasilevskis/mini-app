# App ports on the host machine
FRONTEND_PORT=3000
BACKEND_PORT=2567

# Colyseus WebSocket URL as seen by the browser (host, not Docker service name)
VITE_COLYSEUS_URL=ws://

# Backend (used inside containers via docker-compose; override for local-only runs)
PORT=2567
FRONTEND_URL=http://
